{{{#!text_markdown
```Java
            public void beginTrans( ) {
                    …
                conn = getIdleConnectionFromPoll( );
                userId = getUserId( );
                registerTrans(userId, conn);
                    …
            } 
```
}}}
